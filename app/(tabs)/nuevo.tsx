import { View } from 'react-native';

// Esta pantalla nunca se muestra: el tabPress en _layout.tsx
// intercepta la navegación y abre /nuevo-reporte como modal.
export default function NuevoTab() {
  return <View />;
}
